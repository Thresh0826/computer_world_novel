# 林川

## 基础信息

姓名：林川

性别：男

年龄：旧稿Canon记为24岁；重构前三章未明示，待后续重构正文确认

来源：地球 / 中国现代社会

当前所在地：重构正文中为荒野石坡与溪流附近；

公开职业：软件行业的程序员/具体未公开

## 能力与装备

具有以计算机技术形式理解 / 操作世界部分规则的异常能力；目前已进入正文的体系为 Linux、Python。具体细节【待深度整理】。

主要装备：手机、胸前工牌；重构前三章未确认武器和本地装备。

## Canon状态

【重构正文已确认】

* 来自地球现代办公室，工作与服务器、终端、Linux/Python相关。
* 手机时间停在23:17，信号与Wi-Fi均不可用；记忆中有 `connection established` 与 `Welcome.`。
* 能以类似执行函数的方式调用 `quit()`、`ls`、`fire()`、`fire(2)`、`fire_ball()`。
* 行事倾向为先观察、记录、试验，再行动。
* `quit()`使他从黑红异常区域脱离；`ls`提供草地、石坡、溪流、野兽痕迹、界蚀等名称/判断；`fire()`点燃木柴；`fire(2)`生成更大的短暂火团；两次 `fire_ball()`用于攻击并造成明显精神/注意力消耗。
* 第3章结尾与携灯、持剑/弓/长柄武器的本地人接触，语言仍无法理解。

## 未发布重构进展

* 第4章首次使用 `Translate()` 与 `Live_Translation()`；前者只翻译单句，后者支持连续理解和表达，但会持续占用部分精神。
* 第5章首次使用 `ps`读取目标正在运行的魔法，并用 `kill`中断银背狼的“硬化防御”和“躯体强壮”。
* 发现 `kill`只终止当次运行，不等于永久消除能力；银背狼能重新施展被中断的术式，且强弱可能变化。
* 以 `fire(2)`为莱恩掉落的匕首附火后近身攻击银背狼，随后受击吐血；章末自检生命70%、精力40%。
* 发现银背狼核心，`ls`显示其状态为“可读取”，尚未实际读取。
* 第6章首次使用 `dash(2)`与 `dash(1)`进行短时冲刺，确认该调用可直接作用于自身并按参数改变速度。
* 返回狼尸处取得银背狼皮、两颗狼牙和核心，并以 `fire()`烤食狼肉补充体力。
* 接触核心时能够调用 `hardening()`、`strengthen()`；手指离开核心后效果消失。
* 随阿尔文商队进入贝恩镇，获得莫里斯名片和五日客栈住宿。
* 章末梦见Python脚本、模糊的 `import`与反复出现的 `Session established.`；醒后核心消失，体内出现陌生能量，机制未明。
